import WaitlistPage from '@/components/WaitlistPage'

export const metadata = {
  title: 'Join the Waitlist | Dineflow',
  description: 'Join the Dineflow launch waitlist as a customer, restaurant, hotel, or cafe partner.',
}

export default function Waitlist() {
  return <WaitlistPage />
}
